name: workflow-events
on: 
  push:
  pull_request: 
  workflow_dispatch: 
  schedule:
    - cron: '0 0 * * *' # every day at midnight
jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: show the trigger
        run: echo "triggred using a(n) ${{github.event_name}} event"
